
services:
  portfolio-app:
    build: .
    container_name: portfolio-container

    ports:
      - "3000:3000"

    environment:
      - NODE_ENV=production
      - PORT=3000

    restart: unless-stopped